id: wifi-llapi-r174-radio-regulatorydomain
name: RegulatoryDomain — WiFi.Radio.{i}.
version: '1.0'
source:
  report: 6.3.0GA_prplware_v403_LLAPI_Test_Report.xlsx
  sheet: Wifi_LLAPI
  row: 174
  object: WiFi.Radio.{i}.
  api: RegulatoryDomain
platform:
  prplos: 4.0.3
  bdk: 6.3.1
hlapi_command: ubus-cli WiFi.Radio.{i}.RegulatoryDomain=FR
llapi_support: Support
implemented_by: Vendor Module
topology:
  devices:
    DUT:
      role: ap
      transport: serial
steps:
- id: step1
  action: exec
  target: DUT
  command: ubus-cli WiFi.Radio.*.RegulatoryDomain?
  note: 'Check default value:'
- id: step2
  action: exec
  target: DUT
  command: ubus-cli WiFi.Radio.*.RegulatoryDomain="FR"
  depends_on: step1
  note: Try change all redio to "FR"
- id: step3
  action: exec
  target: DUT
  command: ubus-cli WiFi.Radio.*.RegulatoryDomain?
  capture: result
  depends_on: step2
  note: 'Check API again:'
pass_criteria:
- field: result.RegulatoryDomain
  operator: contains
  value: FR
  description: RegulatoryDomain 應回傳包含 RegulatoryDomain=FR
verification_command: 'root@prplOS:/# wl -i wl0 country

  #a (#a/0) <unknown>

  root@prplOS:/# wl -i wl1 country

  #a (#a/0) <unknown>

  root@prplOS:/# wl -i wl2 country

  #a (#a/0) <unknown>'
results_reference:
  v4.0.1:
    5g: Fail
    6g: Fail
    2.4g: Fail
    comment: 'BRCM: CSP CS00012439197'
  v4.0.3:
    5g: Pass
    6g: Pass
    2.4g: Pass
    comment: "supported; \nwl -i wl0 country list\nwl -i wl0 contry_rev xx\nubus-cli WiFi.Radio.1.RegulatoryDomain=CA; ubus-cli\
      \ WiFi.Radio.1.Vendor.Brcm.RegulatoryDomainRev=170"
