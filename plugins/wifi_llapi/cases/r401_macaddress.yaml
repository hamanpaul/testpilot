id: wifi-llapi-r401-accesspoint_associateddevice_affiliatedsta-macaddress
name: MACAddress — WiFi.AccessPoint.{i}.AssociatedDevice.{i}.AffiliatedSTA.​{i}.
version: '1.0'
source:
  report: 6.3.0GA_prplware_v403_LLAPI_Test_Report.xlsx
  sheet: Wifi_LLAPI
  row: 401
  object: WiFi.AccessPoint.{i}.AssociatedDevice.{i}.AffiliatedSTA.​{i}.
  api: MACAddress
platform:
  prplos: 4.0.3
  bdk: 6.3.1
hlapi_command: ubus-cli WiFi.AccessPoint.{i}.​AssociatedDevice.{i}.AffiliatedSTA.​{i}.MACAddress
llapi_support: Support
implemented_by: pWHM
topology:
  devices:
    DUT:
      role: ap
      transport: serial
    STA:
      role: sta
      transport: adb
      config:
      - iface: 5g
        mode: sta
        ssid: '{{SSID_5G}}'
        key: '{{KEY_5G}}'
  links:
  - from: STA
    to: DUT
    band: 5g
steps:
- id: step1
  action: exec
  target: DUT
  command: Connect WiFi7 Station to GW
- id: step2
  action: exec
  target: DUT
  command: ubus-cli WiFi.AccessPoint.*.? | grep -E 'Affiliated|Active|MACAdd
  capture: result
  depends_on: step1
  note: Check AffiliatedDevice MACAddress
pass_criteria:
- field: result
  operator: not_contains
  value: error
  description: MACAddress 執行不應回傳 error
verification_command: 'iw dev wl0 station dump


  root@prplOS:/# iw dev wl0 station dump | grep Station

  Station e6:d7:d3:ea:cf:15 (on wl0)

  root@prplOS:/# iw dev wl1 station dump | grep Station

  Station 38:06:e6:92:b0:4a (on wl1)

  root@prplOS:/# iw dev wl2 station dump | grep Station

  Station 7e:04:60:d8:fc:6e (on wl2)

  root@prplOS:/#'
results_reference:
  v4.0.1:
    5g: Pass
    6g: Pass
    2.4g: Fail
    comment: 'ARC : Affiliated station not shown in 2.4G '
  v4.0.3:
    5g: pass
    6g: pass
    2.4g: pass
    comment: ''
