id: d145-endpoint-presharedkey
name: D145 EndPoint PreSharedKey
source:
  row: 133
  object: WiFi.EndPoint.{i}.Profile.WPS.Security.
  api: PreSharedKey
bands:
- 5g
- 6g
- 2.4g
llapi_support: Not Supported
topology:
  devices:
    DUT:
      role: ap
      transport: serial
      selector: COM1
steps:
- id: step1_endpoint_probe
  target: dut
  action: read
  command: ubus-cli "WiFi.EndPoint.?"
  expected: No data found
  description: EndPoint tree 不存在，PreSharedKey 無法讀取
pass_criteria:
- field: step1_endpoint_probe
  operator: contains
  value: No data found
