id: wifi-llapi-r184-radio-getradioairstats
name: getRadioAirStats() — WiFi.Radio.{i}.
version: '1.0'
source:
  report: 6.3.0GA_prplware_v403_LLAPI_Test_Report.xlsx
  sheet: Wifi_LLAPI
  row: 184
  object: WiFi.Radio.{i}.
  api: getRadioAirStats()
platform:
  prplos: 4.0.3
  bdk: 6.3.1
hlapi_command: 'ubus-cli WiFi.Radio.{i}.getRadioAirStats() FreeTime : 89,'
llapi_support: Support
implemented_by: pWHM
topology:
  devices:
    DUT:
      role: ap
      transport: serial
    EndpointPC:
      role: endpoint
      transport: ssh
  links:
  - from: DUT
    to: EndpointPC
    interface: eth1
steps:
- id: step1
  action: exec
  target: DUT
  command: ubus-cli "WiFi.Radio.*.getRadioAirStats()"
  capture: result
  note: Connect 2 STAs to radio, use iperf3 to transmit some data between two STAs.
pass_criteria:
- field: result
  operator: not_contains
  value: error
  description: getRadioAirStats() 執行不應回傳 error
verification_command: 'root@prplOS:/# iw dev wl0 survey dump

  root@prplOS:/# iw dev wl1 survey dump

  root@prplOS:/# iw dev wl2 survey dump'
results_reference:
  v4.0.1:
    5g: Fail
    6g: Fail
    2.4g: Fail
    comment: 'ARC : The value is always 0.'
  v4.0.3:
    5g: Fail
    6g: Fail
    2.4g: Fail
    comment: need to support survey in driver level(not only in  wl_cfg80211)
